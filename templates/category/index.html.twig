{# 
   Page de liste des catégories (category/index.html.twig)
   ----------------------------------------------------------
   - Affichage en tableau Bootstrap
   - Actions : voir, modifier, supprimer (réservé aux admins)
   - Bouton ajouter visible uniquement pour les admins
#}

{% extends 'base.html.twig' %}

{% block title %}Catégories{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h1 class="text-primary mb-0">Liste des catégories</h1>
        {% if is_granted('ROLE_ADMIN') %}
        <a href="{{ path('app_category_create') }}" class="btn btn-success">Ajouter une catégorie</a>
        {% endif %}
    </div>
    <div class="row">
        <div class="col-12">
            <div class="card shadow">
                <div class="card-body">
                    <table class="table table-bordered table-hover table-striped align-middle mb-0">
                        <thead>
                            <tr>
                                <th>Nom</th>
                                <th>Description</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                        {% for category in categories %}
                            <tr>
                                <td>{{ category.nom }}</td>
                                <td>{{ category.description }}</td>
                                <td>
                                    <a href="{{ path('app_category_show', {id: category.id}) }}" class="btn btn-sm btn-info">Voir</a>
                                    {% if is_granted('ROLE_ADMIN') %}
                                    <a href="{{ path('app_category_edit', {id: category.id}) }}" class="btn btn-sm btn-warning">Modifier</a>
                                    <form method="post" action="{{ path('app_category_delete', {id: category.id}) }}" style="display:inline-block;" onsubmit="return confirm('Voulez-vous vraiment supprimer cette catégorie ?');">
                                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}">
                                        <button class="btn btn-sm btn-danger">Supprimer</button>
                                    </form>
                                    {% endif %}
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="3" class="text-center">Aucune catégorie trouvée.</td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
